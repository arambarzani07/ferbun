name: Build unsigned iOS IPA

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - .github/workflows/unsigned-ios.yml

permissions:
  contents: read

jobs:
  unsigned-ios:
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Generate native iOS project
        env:
          CI: '1'
        run: npx expo prebuild --platform ios --no-install
      - name: Install CocoaPods
        run: |
          cd ios
          pod install
      - name: Build for physical iPhone without signing
        run: |
          set -euo pipefail
          WORKSPACE="$(find ios -maxdepth 1 -name '*.xcworkspace' -print -quit)"
          test -n "$WORKSPACE"
          SCHEME="$(xcodebuild -list -json -workspace "$WORKSPACE" | python3 -c 'import json,sys; print(json.load(sys.stdin)["workspace"]["schemes"][0])')"
          xcodebuild -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release -destination 'generic/platform=iOS' -derivedDataPath "$PWD/build/derived" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build
      - name: Package unsigned IPA
        run: |
          set -euo pipefail
          mkdir -p dist/Payload
          APP="$(find build/derived/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -print -quit)"
          test -n "$APP"
          cp -R "$APP" dist/Payload/
          (cd dist && zip -q -r -y Ferbun-1.5.4-unsigned.ipa Payload)
          unzip -t dist/Ferbun-1.5.4-unsigned.ipa
          if codesign --verify --deep --strict "$APP" >/dev/null 2>&1; then
            echo 'Expected an unsigned application' >&2
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: Ferbun-1.5.4-unsigned-ipa
          path: dist/Ferbun-1.5.4-unsigned.ipa
          if-no-files-found: error
          retention-days: 30
